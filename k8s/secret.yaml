apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: leaderboard
type: Opaque
data:
  # These are base64 encoded
  # Currently empty - add if you need passwords later
  redis-password: ""
